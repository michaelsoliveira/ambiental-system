'use client'

import type { CSSProperties } from 'react'

/** Sem floating toolbar no financeiro — retorna undefined. */
export function useFloatingToolbarInsetStyle(): CSSProperties | undefined {
  return undefined
}
