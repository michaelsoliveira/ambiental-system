# Produção com TLS (após Let's Encrypt).
# Renomeie para default.conf ou copie: cp docker/nginx/default.ssl.conf docker/nginx/default.conf

limit_req_zone $binary_remote_addr zone=financeiro_api:10m rate=20r/s;

ssl_session_timeout 1d;
ssl_session_cache shared:SSL:50m;
ssl_session_tickets off;
ssl_protocols TLSv1.2 TLSv1.3;
ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
ssl_prefer_server_ciphers off;
add_header Strict-Transport-Security "max-age=63072000" always;

# HTTP → HTTPS + ACME
server {
    listen 80;
    server_name api-financeiro.bomanejo.com.br financeiro.bomanejo.com.br;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# API Fastify
server {
    listen 443 ssl;
    http2 on;
    client_max_body_size 50M;
    server_name api-financeiro.bomanejo.com.br;

    ssl_certificate /etc/letsencrypt/live/api-financeiro.bomanejo.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api-financeiro.bomanejo.com.br/privkey.pem;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        limit_req zone=financeiro_api burst=40 nodelay;
        proxy_pass http://api:3333;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_redirect off;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# Next.js financeiro
server {
    listen 443 ssl;
    http2 on;
    client_max_body_size 50M;
    server_name financeiro.bomanejo.com.br;

    ssl_certificate /etc/letsencrypt/live/api-financeiro.bomanejo.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api-financeiro.bomanejo.com.br/privkey.pem;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://financeiro:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_redirect off;
    }
}
